# ROADMAP · 从基础版到完整版的六个里程碑

> 适用起点(本手册假设项目已具备):视频分析链已通(拍摄/相册导入 → 姿态 → 挥拍切分 → AI 点评/评级 → 报告),
> 训练库 100 套、错误库 110 种已入包,球友圈/球馆初版已有,AI 走服务器代理零配置。
> 终点:下述 M1-M6 全部落地。每个里程碑先读标注的 PITFALLS 条目再动手。

---

## M1 · 信息架构重构:4 Tab + 一分钟体验链

**为什么先做这个**:老版 Tab 杂乱,新用户前 15 分钟的认知负担是第一优先级。

任务方向:
- Tab 收敛为 4 个:**学**(课程主线)/**练**(拍摄+记录+趋势三分段)/**球友圈**(动态+球馆)/**我的**;
- 「练」是全屏相机页,分段切换时相机必须保活(ZStack 常驻底层调透明度,不能 if/else 销毁重建);
- **示例视频入口全局提权**:内置一段示例挥拍视频+预生成分析结果,没到球场的人第一分钟就能看到完整分析链。
  入口至少四处:记录空态、拍摄页、引导完成页(主按钮)、学 Tab 任务卡;
- 新手任务卡(4 步:生成课程/学完第一课/示例分析/设一次战术板),完成自动打勾,可关闭。

验收:切 Tab 不卡;录制中切去记录页再回来,录制没断;示例入口从任一处点进都能看到完整报告。

高危坑:PITFALLS #1(工程文件注册)、#7(相机保活与息屏)、#8(通知深链与 @Published 重放)、#12(iPad 限宽)。

---

## M2 · 课程引擎:画像 → 秒配 → 12 课打卡闭环

任务方向:
- 用户画像模型(水平/性别/持拍手/年龄/身高体重/每周天数/场地/搭档/目标);
- **模板池秒配**:常见画像组合预生成课程模板,命中即秒出(不让用户等 AI);
  罕见画像先给标准模板、后台静默跑 AI 定制,完成后课程页横幅提示切换;
- 课程结构:3 阶段 × 12 课,每课=训练方法库里挑的动作组合+知识讲堂关联章节;
- 课节详情:分步勾选 → 完成打卡 → 连续天数统计;完成弹窗给"去拍一段检验"的跳转;
- 「AI 微调」:用户一句话(如"膝盖不好,少跳")→ 重排课程;
- 知识讲堂 30 讲(规则/道理/战术)+ 战术板(赛前定战术、赛后核查)一并挂到学 Tab。

验收:新用户从选水平到看到 12 课 ≤ 5 秒;打卡后连续天数正确;AI 微调后课程真的变了。

高危坑:#9(AI 强 JSON 输出)、#10(AI 请求超时与重试)、#11(内容库随行 prompt 的成本意识)。

---

## M3 · 社区 v2:评论/配图/私聊/Apple 登录

任务方向:
- 发帖四类型(约球/求指点/晒战报/球馆点评)+ 城市/球馆/联系方式选填;
- **评论系统** + 点赞(必须乐观更新:本地先变、失败回滚)+ Pow 爱心迸发动效;
- **帖子配图**:客户端压缩(长边 1280/JPEG0.8)→ 先传图拿文件名 → 发帖带文件名;
  服务端校验"本设备上传过且文件存在"才入库;
- **图片九宫格 + 全屏浏览器**:单图大图(4:3 裁切),多图三等分;点开全屏(双击缩放/捏合 1-5x/下滑关闭背景渐透/页码胶囊),纯 SwiftUI 自写,零依赖;
- **应用内私聊**:会话列表+轮询拉取,从帖子"私聊 TA"发起;
- **Sign in with Apple**:浏览免登录,发帖/评论/私聊需登录;服务端验签 identityToken;
- UGC 合规(上架硬要求):举报(多人举报自动隐藏)、屏蔽用户(本地过滤)、发帖同意条款、内容脏词过滤。

验收:发带图帖全流程 ≤ 30 秒;断网时每一步都有明确错误提示;点赞零延迟感。

高危坑:#13(点赞乐观更新)、#14(评论输入框吸底)、#15(图片浏览器手势冲突)、#16(PhotosPicker 大文件)、#22-#26(后端安全五条)。

---

## M4 · 趋势页重做 + 全局视觉系统

任务方向:
- 趋势页四件套:本周目标环(周练 N 天)+ 连续天数/8 周热力格 + 个人纪录卡(最快挥速/单场最多拍/累计/场次)+ 训练量柱状图;
- 数据口径:课程打卡日 ∪ 训练活跃日,示例数据必须排除在统计外;
- 全局视觉:统一品牌绿、圆角卡片、空态插画;深浅色两套都要过一遍(禁止硬编码黑白);
- 生成式插画融入课程页/知识讲堂/战术板(素材包见 ASSETS.md,"语义贴合才用,宁缺毋滥")。

验收:深色/浅色模式各过一遍无死白死黑;iPhone + iPad 都不破版;示例数据不进任何统计。

高危坑:#12(iPad maxWidth 教训)、#17(Charts 大数据分桶)、#18(空态/错误态覆盖)。

---

## M5 · 五幕新手引导(转化率关键)

任务方向:
- 欢迎页 → 选水平(1 次点选)→ 五幕引导,幕间右滑入/淡出转场,深浅色自适应渐变背景;
- **第 1 幕**:真实示例视频+骨架叠加播放 → 播完弹出真实分析报告卡(评分/维度/结论,用预生成数据);
- **第 2 幕**:「关于你 · 30 秒」资料卡(性别/持拍手/年龄/身高/每周天数)融合在"生成我的课程"里,
  **绝不做成独立整页表单**(实测反感);点生成 → 课卡瀑布动画,用的就是真实秒配结果;
- **第 3 幕**:手表功能演示(表盘 mockup + 演示数据动效 + 特性卡),注明"演示数据、无表也能用";
- **第 4 幕**:趋势打卡演示(点一下打卡 → 热力格点亮);
- **第 5 幕**:球友圈演示(点发布 → 动效)→ 完成页(主按钮=先看一次示例分析);
- 冷启动加启动屏(纯色+Logo),避免白屏几秒。

验收:全程可跳过;跳过的用户也有默认课程;从欢迎页到主界面 ≤ 90 秒(不含看视频)。

高危坑:#19(视频播放器在引导里的生命周期)、#20(默认值陷阱:选项数组换序导致默认值漂移)、#12。

---

## M6 · 手表双端(深水区,最能证明工程能力)

任务方向(按难度递增,做到哪算哪,每步都可独立交付):
1. **手表独立训练**:CoreMotion 挥拍检测(峰值角速度+滞回+不应期)、HealthKit 心率、
   实时读数(拍数/心率/上拍峰速)、结束小结页(拍数/时长/心率,防挂起用 UserDefaults 兜底);
2. **双通道协议**:实时 sendMessage + 断连 transferUserInfo 排队补传;所有出站消息盖"场次键"
   (手表侧训练开始时间戳),接收端按场次键归属,**绝不按"当前正在训练"推断**;
3. **快练落库**:手表单独练的场次回手机自动落成档案(拍数/挥速/心率/图表)+ 纯数据 AI 点评;
   落库必须幂等(事件按 id 去重、心率按时间戳去重、原子写),晚到/重复/乱序全部无害;
4. **腕上遥控开拍**:手表让手机开录。必须做成 sendMessage+replyHandler 握手:手机应答 ok/原因
   (锁屏中/不在拍摄页),手表按应答给指引——fire-and-forget 版本会大量无声失败,宁可不做;
5. 锦上添花:手机实时仪表盘(手表练、手机当大屏)、AI 点评头号问题推送到表盘、逐拍触觉分级。

验收:模拟器两端 build 绿;配对模拟器上手表开练手机能收到实时数据;
杀掉手机 App → 手表独立练完 → 重开手机 → 快练档案自动出现(这是黄金用例)。

高危坑:**#27-#33 手表全章必读**,这是全项目死过最多次的地方。
