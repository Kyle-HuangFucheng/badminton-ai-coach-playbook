# PITFALLS · 33 条实战踩坑手册

> 每条 = **现象 → 根因 → 正确做法**。全部真实踩过。写代码前按 ROADMAP 里程碑标注的编号查阅。

## 一、工程与构建

**#1 新增 Swift 文件编译不进去 / "Cannot find X in scope"**
现象:新建文件后 build 报找不到类型,或改动"没生效"。
根因:工程用传统 PBXFileReference(非文件夹同步组),新文件必须注册进 xcodeproj。
做法:用 xcodegen 维护 project.yml 的话,**每次增删文件后重跑 `xcodegen generate`**;手改 pbxproj 则要加满 4 处(PBXBuildFile/PBXFileReference/PBXGroup children/PBXSourcesBuildPhase)。改完立刻 build 验证,别攒。

**#2 增量构建吃掉资源更新**
现象:替换了 bundle 里的视频/JSON,跑起来还是旧的。
做法:换过 Resources 之后,发截图/交付前必须 clean build 一次。

**#3 watchOS 部署目标被 Xcode "Recommended" 悄悄改浮动**
现象:手表 build 突然崩/警告一堆。
做法:部署目标写死数值,别点 Xcode 的推荐迁移。

**#4 App 图标带 alpha 通道**
现象:上传 App Store 被拒。
做法:图标出图后统一去 alpha(JPEG 转一遍或 sips 去通道)。

## 二、SwiftUI / UIKit

**#5 sheet 里保存数据,父视图不刷新**
做法:sheet 子视图用回调把数据交回父视图,由父视图自己写库;别在 sheet 里直接 insert。

**#6 NavigationStack 里声明式 NavigationLink 和值驱动 path 混用,页面秒弹回**
做法:一个 Stack 只用一种导航方式;列表页统一 `NavigationStack(path:)` 值驱动。

**#7 全屏相机页:切页销毁 / 息屏断录**
现象:分段切换后录制中断;架手机拍着拍着屏幕锁了,录像断。
做法:相机视图常驻 ZStack 底层,切段只调 opacity 和 allowsHitTesting;录制中或拍摄段激活时 `isIdleTimerDisabled = true`,并以"当前是否激活"为准回收,防全 App 常亮耗电。

**#8 通知/跨 Tab 深链收不到事件**
现象:点通知要跳的页面还没建出来,事件丢了。
做法:用 `@Published` 通道(新订阅者会重放当前值),目标页迟一拍创建也能收到;处理完记得置 nil 防重复触发。

**#9 强 JSON 输出的 AI 返回解析失败**
现象:AI 返回带 markdown 代码块/前后废话,JSON 解析崩。
做法:prompt 明说"只输出一个 JSON 对象,不要 markdown";解析用三层兜底:严格解析 → 抠最外层大括号 → 失败给用户可读错误+重试按钮。**解析失败也计费,max_tokens 给足**(中文 JSON 很容易超 900,给 1500),截断=白花钱。

**#10 真机 AI 请求 -1005 连接中断**
做法:URLSession 请求加超时(90s)+失败自动重试一次;错误文案区分"网络断了"和"服务器忙"。

**#11 内容库整库随行 prompt**
认知:错误库+训练库目录随每次点评发送(约 8-9K 字),这是设计选择——换来 AI 只能从库里挑编号、不会瞎编。输出费率通常是输入的 6 倍,省成本先压输出(紧凑 JSON),不是砍输入。

**#12 iPad 上 UI 摊满全屏 / 背景被压成卡片**
现象:iPhone 好好的,iPad 上按钮拉满屏宽;或者给整个带背景的 VStack 限宽,iPad 四周黑边。
做法:`maxWidth` 只限**内容**,不限背景层;背景 `.ignoresSafeArea()` 铺满,内容另包一层 `.frame(maxWidth: 520).frame(maxWidth: .infinity)`。iPad 顶部 TabBar 悬浮条会和自定义顶部控件重叠,regular 尺寸类要下移让位。

**#13 点赞等高频互动等网络返回才更新**
做法:乐观更新——本地立刻 +1 变红,请求失败再回滚并提示。信息流的延迟感是最廉价的减分项。

**#14 评论输入框被键盘顶飞/够不着**
做法:`safeAreaInset(edge: .bottom)` 常驻底部,天然避键盘。

**#15 全屏图片浏览器手势打架**
现象:缩放/翻页/下滑关闭互相抢手势。
做法:TabView(.page) 管横向翻页;捏合用 MagnificationGesture 增量累乘钳制 1-5x;拖拽区分两态——放大时平移(钳制边界防拖出黑边),未放大时"竖直分量明显大于水平"才当下滑关闭,背景透明度随位移线性衰减;常驻右上角 X 兜底。

**#16 相册导入大视频把内存打爆 / 全程无进度**
做法:视频用文件级 Transferable(拿 URL 拷贝),别 `loadTransferable(Data.self)` 整段进内存;导入/分析全程要有进度文案,静默会被当成"App 坏了"。

**#17 图表逐拍画几百根柱子**
做法:超过 ~80 个数据点先分桶聚合再画;时间轴用分桶均值。

**#18 空态≠错误态**
现象:断网时列表显示"还没有内容",误导用户。
做法:每个网络列表三态齐全:加载中/错误(带重试按钮)/真空态(带引导动作)。errorMessage 拿到了就必须展示,别写进不显示的变量。

**#19 引导页里的视频播放器**
做法:AVPlayer 放 UIViewRepresentable 里时,布局回调里包 `CATransaction.setDisableActions(true)` 防隐式动画抖动;幕切换时暂停并释放;播放完成用通知回调推进流程,别用定时器猜时长。

**#20 选项数组换序,默认值跟着漂移**
现象:把「左手|右手」显示顺序对调后,所有没手动选的用户默认变成了左手。
根因:默认值写的 `options[0]`。
做法:**默认值写字面量**("右手"),数组只管显示顺序。任何"UI 顺序调整"都要检查有没有代码在依赖顺序取默认。

**#21 日期格式当标题**
做法:记录列表/详情标题别用裸日期字符串,给语义名(如"示例 · 正手高远球 5 拍"),日期放副标题。

## 三、后端与安全(FastAPI + SQLite)

**#22 SQLite 多进程写冲突**
做法:uvicorn **单 worker** 部署;所有连接开 WAL;限额等内存计数器在多 worker 下会各算各的,别加 worker。

**#23 上传文件与身份归属**
做法:上传落盘文件名用 `secrets.token_hex`(不可枚举);入库登记 (file, device_id);发帖引用图片时校验"本设备上传过且文件存在"。头像/媒体接口同样先校验归属再写。

**#24 每日限额防刷**
做法:AI 代理按设备每日限额 + 全站每日总量双闸;X-Forwarded-For 在无可信反代时完全由客户端控制,**不能**拿来限流;直连部署用 request.client.host。

**#25 删除/举报类接口的权限**
做法:删除=本人或管理员令牌;举报要先确认目标存在再计数,防对任意 id 灌举报;多人举报自动隐藏后要有管理员复核路径。

**#26 客户端表列迁移**
做法:SQLite 老库加列用 `PRAGMA table_info` 检查后 ALTER TABLE,`CREATE TABLE IF NOT EXISTS` 不会更新老库结构。

## 四、手表双端(全项目死过最多次的地方,M6 前必读)

**#27 WCSession 回调线程**
现象:UI 状态错乱、"Publishing changes from background threads"紫色警告、甚至死锁。
根因:所有 WCSession delegate 回调都在**非主线程**。
做法:回调里改任何 @Published/UI 状态一律 `DispatchQueue.main.async`;**严禁 main.sync**(和主线程互等=死锁,真实发生过);带 replyHandler 的回调,async 到主线程处理后再调用 escaping 的 replyHandler 回传。

**#28 phoneReachable ≠ 手机可用**
现象:手表看手机"可达",发指令却石沉大海。
根因:手机锁屏/后台时,手表→手机方向依然 reachable(系统特权)。
做法:遥控类指令必须 sendMessage+replyHandler 握手,手机应答 ok/失败原因(锁屏中/不在目标页),超时给"手机没响应"。fire-and-forget 的遥控等于埋雷。

**#29 排队补传的指令晚到**
现象:断连期间 transferUserInfo 排队的"开始训练"指令,重连后突然让手表开始一场几小时前的训练。
做法:所有会话指令带时间戳;**开始类**指令超过 30s 视为陈旧丢弃;**停止类**指令必须尽量兑现(只丢弃比当前场次开始还早的),否则断连后手表空转狂耗电。

**#30 "当前场次"靠推断必出脏数据**
现象:上一场的补传事件写进了下一场(甚至写进旧视频的档案)。
做法:场次归属靠**场次键**(训练开始时刻的时间戳)盖章在每条出站消息上,接收端按键归属;换场次时把上一场的接收上下文(事件数组/导出指针/场次键)全部显式清空——"陈旧指令不处理"不等于"不清状态",状态重置和 UI 副作用要分开判断。

**#31 落库必须幂等**
场景:同一场数据会经 实时通道+补传通道+重放 到达多次、乱序、分批。
做法:统一 upsert——事件按 id 去重、心率按时间戳去重、合并后原子写;结束时间用"手表真实停表时刻",拿不到就用最后一拍兜底,**绝不用"落盘时刻"**(会把补传延迟算成训练时长)。

**#32 双时钟对齐**
现象:手表挥拍时间戳和手机视频时间轴对不上,逐拍切片错位。
做法:开录时做 ping-pong 对时(取最小 RTT 的偏移),手表事件带手表时钟,手机侧用偏移换算;对时器每场重建,跨场沿用会锁死过期偏移。

**#33 手表 App 挂起丢结果**
现象:训练结束的小结页,回头再看没了。
做法:结束时先把小结快照(拍数/时长/心率)存 UserDefaults,启动时检查未确认小结则恢复;停止流程里"先取快照、再停传感器",顺序反了快照是空的。
